// Backend API base URL. This file can be edited directly after build without
// recompiling. Leave it empty to use the relative path "/api/v1" (same-origin
// or reverse-proxy deployments). For a backend on a separate host/port, set the
// full URL, e.g.:
//   window.__ENV__ = { API_BASE_URL: 'http://192.168.1.10:8081/api/v1' }
window.__ENV__ = { API_BASE_URL: '' }
